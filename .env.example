# Public API (defaults to localhost:8080 in dev if unset)
NEXT_PUBLIC_API_URL=http://localhost:8080

# W3Forms — https://web3forms.com (client-side key, safe to expose)
NEXT_PUBLIC_W3FORMS_ACCESS_KEY=
