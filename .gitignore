# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Dependencies
node_modules/

# Next.js
.next/
out/

# OpenNext / Cloudflare
.open-next/
.wrangler/
.dev.vars
cloudflare-env.d.ts

# Env / secrets
.env
.env.*
!.env.example
# .env.production holds only NEXT_PUBLIC_API_URL — a public value that ends
# up inlined in the client bundle regardless. Tracked so a fresh clone/CI
# build doesn't silently fall back to localhost.
!.env.production

# Build / tooling
*.tsbuildinfo

# OS / IDE
.DS_Store
.idea/
.vscode/
*.log
